import 'package:flutter/material.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}

// Material App adalah library yang berisi widget — widget untuk membuat aplikasi. 
//Ada banyak properti untuk membuat tampilan mobile.
